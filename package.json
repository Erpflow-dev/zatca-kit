{
  "name": "@erpflow/zatca-kit",
  "version": "1.0.0",
  "private": true,
  "description": "Reusable ZATCA (Fatoora) e-invoicing kit: API client, EGS CSR generator, CSID onboarding reference, golden-file CI",
  "main": "src/fatoora-client.ts",
  "scripts": {
    "test": "jest --runInBand",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "roots": [
      "<rootDir>/src"
    ]
  }
}
